# Notebooks

Colab-ready notebooks implementing the end-to-end continual fine-tuning and membership inference evaluation workflow.

Run them sequentially (00–12) or orchestrate via `12_run_sweep_driver.ipynb`.
